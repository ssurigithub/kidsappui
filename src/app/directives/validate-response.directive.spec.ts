import { ValidateResponseDirective } from './validate-response.directive';

describe('ValidateResponseDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateResponseDirective();
    expect(directive).toBeTruthy();
  });
});
